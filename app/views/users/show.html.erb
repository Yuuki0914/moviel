<h1>マイページ</h1>
<p>名前 : <%= @user.name %></p>
<p>メールアドレス : <%= @user.email %></p>
<p>プロフィール : <%= @user.profile %></p>

<% if current_user.id == @user.id %>
    <%= link_to "編集する", edit_user_registration_path %>
    <%= image_tag @user.image_url, size: "250x200" if @user.image? %>
<% end %>

<h2>ユーザーの投稿一覧</h2>
<% @user.movies.each do |t| %>
    <%= t.user.name %>
    <%= t.title %>
    <%= t.highlight %>
<% end %>

<!-- 以下はいいね機能を実装している場合-->
<h2>ユーザーがいいねした投稿一覧</h2>
<% @user.liked_movies.each do |t| %>
    <%= t.user.name %>
    <p>『タイトル』</p>
    <%= t.title %>
    <p>『あらすじ』</p>
    <%= t.highlight %>
<% end %>
<!-- ここまで-->